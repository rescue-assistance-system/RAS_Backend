export enum CaseStatus {
    COMPLETED = 'completed',
    CANCELLED = 'cancelled',
    PENDING = 'pending',
    READY = 'ready',
    ACCEPTED = 'accepted',
    EXPIRED = 'expired',
    SAFE = 'safe'
}
